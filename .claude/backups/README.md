# backups/

Internal backups of `.claude.json` project config files, created automatically by Claude Code before overwriting. Files are named with timestamps (e.g., `.claude.json.backup.1774373346271`).

**Excluded from version control** — ephemeral, auto-generated.
