# projects/

Per-project session data. Each subdirectory is named after a project path (e.g., `D--wa-git-cfv-checksum-tool`) and contains:

- Session transcript JSONL files (full conversation logs)
- `sessions-index.json` (session metadata index)

**Excluded from version control** — contains full conversation transcripts which may include sensitive context.
